{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "request": "launch",
            "name": "Launch Server",
            "command": "${workspaceFolder}/start.sh"
        }
    ]
}